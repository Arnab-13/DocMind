This is a simple test document for a RAG loader. The loader should read this text, split it into chunks, generate embeddings, and store the resulting vectors for retrieval.

Test facts:

The product name is AcmeAI.
AcmeAI was created in 2024.
The default maximum chunk size is 500 characters.
The supported file formats are TXT, PDF, and Markdown.
The test environment is called RAG-Test.
